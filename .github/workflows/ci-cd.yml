name: CI/CD

on:
  push:
    branches:
      - develop
      - master

jobs:
  build:

    runs-on: ubuntu-latest
    container: python:3

    services:
      postgres:
        image: postgres:12.6
        env:
          POSTGRES_DB: myshows
          POSTGRES_USER: myshowsuser
          POSTGRES_PASSWORD: myshowsuser
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        # Add a health check
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
   
#    strategy:
#      max-parallel: 4
#      matrix:
#        python-version: [3.7, 3.8, 3.9]

    steps:
    - uses: actions/checkout@v2
    - name: Cache dependency # caching dependency will make our build faster.
      uses: actions/cache@v2 
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
#    - name: Set up Python ${{ matrix.python-version }}
#      uses: actions/setup-python@v2
#      with:
#        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
#        pip install pylint-django
    - name: Run Migrations 
      run: | 
        python myshows/manage.py makemigrations
        python myshows/manage.py migrate
    - name: Run Test
      run: python myshows/manage.py test    
#    - name: Analysing the code with pylint
#      run: |
#        pylint --load-plugins pylint_django --django-settings-module=myshows.settings  myshows/*